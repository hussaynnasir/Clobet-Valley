using Settings.Interfaces;
using UiViewController;

namespace Settings.View
{
    public class SettingsView : BaseUiView<BaseUiViewRefs>, ISettingsView
    {
        protected override void Show(BaseUiViewDataModel model)
        {
            base.Show(model);
        }
    }
}